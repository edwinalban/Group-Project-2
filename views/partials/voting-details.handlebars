<section class="card">
    <header class="animal-name">{{name}}</header>
    <div class="card-body">
        <img src="/assets/images/{{filename}}" alt="{{description}}">
    </div>
    <button class="voting-btn">Vote for me!</button>
    <div>
        {{voteCount}}
    </div>
</section>
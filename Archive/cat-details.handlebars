<section class="card">
  <a href="/cat/{{id}}"></a>
    {{!-- <header class="animal-name">{{name}}</header> --}}
<h1 class="animal-name">{{name}}</h1>
    <div class="card-body">
      <p>testing to see where this goes</p>
      
      <img src="/assets/images/{{filename}}" alt="{{description}}" style="border: 5px inset black; width: 30vw; height: auto">
    </div>
  
</section>

{{!-- <section class="card">
  <a href="/cat/{{id}}">
    
  <h1 class="cat-name">{{animal.name}}</h1>
    <div class="card-body">
      <img src="/assets/images/{{animal.filename}}" alt="{{animal.description}}" style="border: 5px inset black; width: 30vw; height: auto">
      <p>Age:{{animal.age}} years old</p>
      <p>Sex:{{animal.sex}}</p>
      <p>Breed: {{animal.breed}}</p>
      <p>Special Needs:{{animal.specialNeeds}}</p>
      <p>Location: {{animal.location}}</p>
    </div>
  </a>
</section> --}}